version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: qbitleap-solo-miner_dashboard_1
      APP_PORT: 80

  dashboard:
    image: nginx:1.27.5-alpine
    restart: on-failure
    volumes:
      - ./dashboard:/usr/share/nginx/html:ro
