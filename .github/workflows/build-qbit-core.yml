name: Build Qbit Core image

on:
  push:
    branches:
      - main
    paths:
      - "qbitleap-solo-miner/qbit-core/**"
      - ".github/workflows/build-qbit-core.yml"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and publish Qbit Core
        uses: docker/build-push-action@v6
        with:
          context: ./qbitleap-solo-miner/qbit-core
          file: ./qbitleap-solo-miner/qbit-core/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ghcr.io/qbitleap/qbitleap-qbit-core:0.1.0
